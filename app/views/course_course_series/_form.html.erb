<%= form_for(course_course_series) do |f| %>
  <% if course_course_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_course_series.errors.count, "error") %> prohibited this course_course_series from being saved:</h2>

      <ul>
      <% course_course_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %>
    <%= f.text_field :course_id %>
  </div>

  <div class="field">
    <%= f.label :course_series_id %>
    <%= f.text_field :course_series_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
