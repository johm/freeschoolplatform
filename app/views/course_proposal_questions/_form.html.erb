<%= form_for(course_proposal_question) do |f| %>
  <% if course_proposal_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_proposal_question.errors.count, "error") %> prohibited this course_proposal_question from being saved:</h2>

      <ul>
      <% course_proposal_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %>
    <%= f.text_area :question %>
  </div>

  <div class="field">
    <%= f.label :help_text %>
    <%= f.text_area :help_text %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
